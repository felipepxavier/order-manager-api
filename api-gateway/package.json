{
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && sls deploy"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.146",
    "@types/node": "^22.10.2",
    "serverless-offline": "^14.4.0",
    "typescript": "^5.7.2"
  }
}
